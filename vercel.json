{
  "version": 2,
  "installCommand": "npm ci --omit=optional --ignore-scripts",
  "buildCommand": "npm run build",
  "framework": "vite",
  "outputDirectory": "dist",
  "routes": [{ "src": "/(.*)", "dest": "/" }]
}
