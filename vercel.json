{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm ci --no-optional",
  "framework": "vite",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}
